# Glpi-FORMCreator-Leaflet

🚀 Como usar
Copie a pasta mymapform para glpi/plugins/.

Vá em Configurar > Plugins no GLPI e instale/ative o plugin.

No FormCreator, ao criar um formulário, aparecerá um novo tipo de questão chamado “Mapa (Leaflet)”.

O campo renderiza o mapa e grava latitude/longitude como parte da resposta.

No Target do FormCreator, mapeie esses valores para o ticket (ex.: descrição ou campos customizados).
